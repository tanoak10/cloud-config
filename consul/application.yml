server:
  port: 8082
  tomcat:
    uri-encoding: utf-8
spring:
  datasource:
  #端口号更改了
    url: jdbc:mysql://localhost:3308/stu_m?characterEncoding=utf8&useSSL=true
    username: root
    password: 123123
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
logging:
  level: warn
  file: log/stumanage.txt
mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  global-config:
    db-column-underline: true
    id-type: 0
    refresh-mapper: true
    capital-mode: true
  configuration:

    map-underscore-to-camel-case: true